
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>
<br />
<div style="padding-left:100px; width:80%">
    <div class="row">
        <div class="col-6">
            <h2 class=" text-info">Role List</h2>
        </div>
        <div style="padding-left:220px;">
            <a asp-action="Create" class="btn btn-info"><i><i class="fas fa-plus"></i></i>&nbsp; New Role</a>
        </div>
    </div>

    <br />
    <div class="p-4 border rounded">
        <table id="tblData" class="table table-striped table-bordered">
            <thead style="background-color:darkred">
                <tr class="table-info">
                    <th>First Name</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>
</div>
@*<div>
        <table class="table table-striped border" style="width:50%">
            <tr class="table-info">
                <th>Name</th>
                <th>Price</th>
                <th>Color</th>
                <th>ProductType</th>
                <th>SpecialTag</th>
                <th>Availability</th>
                <th></th>
                <th>
                </th>
            </tr>
            @foreach (var item in ViewBag.Roles)
            {
        <tr>
            <td>@item.Name</td>
            <td>
                <partial name="_ButtonPartial" model="@item.Id" />
            </td>
        </tr>
            }
        </table>
    </div>*@



@section Scripts{

    <script src="~/js/role.js"></script>
    <script type="text/javascript">

        $(document).ready(function () {

        var save = '@TempData["save"]'
        if (save != '') {
            alertify.success(save);
            }

        var edit = '@TempData["edit"]'
        if (edit != '') {
            alertify.success(edit);
            }

    })

    </script>
}